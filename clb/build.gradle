buildscript {
    ext {
        gradleRootDir = "${rootProject.getRootDir()}/gradle"
    }

}

ext {
    gradleRootDir = "${rootProject.getRootDir()}/gradle"
    gradleDir = "${rootProject.getRootDir()}/gradle"
}
